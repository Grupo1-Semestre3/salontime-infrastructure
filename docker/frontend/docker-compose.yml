version: '3.8'

services:
  frontend:
    build:
      context: .
      args:
        REPO_URL: FRONTEND_REPO
        BRANCH: FRONTEND_BRANCH
    container_name: frontend
    ports:
      - "FRONTEND_PORT:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
